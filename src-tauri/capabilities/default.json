{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["main", "popover", "nudge"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "notification:default",
    "dialog:default",
    "fs:default"
  ]
}
